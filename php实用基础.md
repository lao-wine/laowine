---
title: php实用基础
---
[toc]
## 环境搭建
`本文使用的环境是win10+phpstorm-2020.3.2+phpstudy_64_8.1.1.2`
## 1、PHP简述
`PHP:超文本预处理器，是一种开源脚本语言。`
在一个PHP文件中可以包括以下内容：
- PHP文件可包含文本、HTML、JavaScript代码和PHP代码
- PHP代码在服务器上执行，可以以纯HTML形式返回给浏览器
- PHP文件默认文件扩展名是“.php”
  
`php的使用：`
- PHP可以生成动态页面内容
- PHP可以创建、打开、读取、写入、关闭服务器上的文件。
- PHP可以收集表单数据
- PHP可以接受和发送cookies
- PHP可以添加、删除、修改数据库中的数据
- PHP可以限制用户访问网站上的一些页面
- PHP也已加密数据
  
  ## 2、基本语法格式
 - PHP脚本以<?php 开始，以?> 结束
 - // 单行注释
 - /*多行注释 */
 - 变量的命名
   > 1、变量以$符号开始，后面跟着变量的名称。
   > 2、变量名必须以字母或者下划线字符开始。
   > 3、变量名只能包含字母数字字符以及下划线（A-z、0-9和_ ）。
   > 4、变量名不能包含空格。

- 变量的声明
  > 1、变量的声明以$开始的。
  > 2、$num = 3.14；
  > 3、$str = “hello”；

- static关键字
   `当一个函数完成时，它的所有变量通常都会被删除。然后，有时候您希望某个局部变量不要被删除。要做到这一点，请在您第一次声明变量时使用static关键字。`
 ```
 <?php
 			function test(){
					static $n=0;
					$n++;
					echo"调用了".$n."\n";
			}
			test();test();
?>
 ```
  ## 3、数据类型、常量以及字符串
  `php有五种数据类型：string、integer、float、boolean、array、obecct、null`
  - 